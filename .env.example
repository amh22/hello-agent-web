# For deployment: set your API key
# For local dev: omit this to use OAuth from ~/.claude/ (if already authenticated)
ANTHROPIC_API_KEY=your-api-key-here
DEMO_PASSWORD=your-secret-password
MAX_BUDGET_USD=1.00
RATE_LIMIT_MAX_REQUESTS=5  # Max requests per minute per IP
