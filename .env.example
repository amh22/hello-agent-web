# Cloudflare Worker URL (required)
# This is the URL of the hello-agent-web-worker deployed to Cloudflare
CLOUDFLARE_WORKER_URL=https://hello-agent-web-worker.your-subdomain.workers.dev
# CLOUDFLARE_WORKER_URL=http://localhost:8787

# Demo password for access control
DEMO_PASSWORD=your-secret-password

# Number of previous questions to include in conversation history (default: 8)
# Each question includes its corresponding assistant response
NEXT_PUBLIC_HISTORY_QUESTIONS=8
